[package]
name = "new-arp-scan"
version = "0.0.2"
edition = "2024"
license = "AGPL-3.0-only"

[dependencies]
clap = { version = "4.5", features = ["derive", "std"] }
libc = "0.2"

[lints.rust]
warnings = "deny"
unsafe_op_in_unsafe_fn = "deny"

[lints.clippy]
pedantic = "warn"
